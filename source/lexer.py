# The file was automatically generated by Lark v0.6.4
#
#
#   Lark Stand-alone Generator Tool
# ----------------------------------
# Generates a stand-alone LALR(1) parser with a standard lexer
#
# Git:    https://github.com/erezsh/lark
# Author: Erez Shinan (erezshin@gmail.com)
#
#
#    >>> LICENSE
#
#    This tool and its generated code use a separate license from Lark.
#
#    It is licensed under GPLv2 or above.
#
#    If you wish to purchase a commercial license for this tool and its
#    generated code, contact me via email.
#
#    This program is free software: you can redistribute it and/or modify
#    it under the terms of the GNU General Public License as published by
#    the Free Software Foundation, either version 2 of the License, or
#    (at your option) any later version.
#
#    This program is distributed in the hope that it will be useful,
#    but WITHOUT ANY WARRANTY; without even the implied warranty of
#    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
#    GNU General Public License for more details.
#
#    See <http://www.gnu.org/licenses/>.
#
#

try:
    from debug_tools import getLogger

except ( ImportError, ValueError ):   # Python 3
    # getLogger is a function, but it returns a class object
    class getLogger(object):
        name = ""
        level = 0
        parent = None
        propagate = False
        handlers = None
        disabled = False
        def __call__(*args, **kwargs): pass
        def __init__(self, name, level=0): pass
        def setLevel(self, level): pass
        def debug(self, msg, *args, **kwargs): pass
        def info(self, msg, *args, **kwargs): pass
        def warning(self, msg, *args, **kwargs): pass
        def warn(self, msg, *args, **kwargs): pass
        def error(self, msg, *args, **kwargs): pass
        def exception(self, msg, *args, **kwargs): pass
        def critical(self, msg, *args, **kwargs): pass
        def log(self, level, msg, *args, **kwargs): pass
        def findCaller(self, stack_info=False): pass
        def makeRecord(self, name, level, fn, lno, msg, args, exc_info, func=None, extra=None, sinfo=None): pass
        def handle(self, record): pass
        def addHandler(self, hdlr): pass
        def removeHandler(self, hdlr): pass
        def hasHandlers(self): pass
        def callHandlers(self, record): pass
        def getEffectiveLevel(self): pass
        def isEnabledFor(self, level): pass
        def getChild(self, suffix): pass

log = getLogger(__name__, 1)

class LarkError(Exception):
    pass

class GrammarError(LarkError):
    pass

class ParseError(LarkError):
    def __eq__(self, other):
        """ Determines whether this object is equal to another one based on their hashes. """
        if isinstance(self, LarkError) is isinstance(other, LarkError):
            return str(self) == str(other)

        raise TypeError("'=' not supported between instances of '%s' and '%s'" % (
                self.__class__.__name__, other.__class__.__name__))

class LexError(LarkError):
    pass

class UnexpectedInput(LarkError):
    pos_in_stream = None

    def get_context(self, text, span=40):
        pos = self.pos_in_stream
        start = max(pos - span, 0)
        end = pos + span
        before = text[start:pos].rsplit('\n', 1)[-1]
        after = text[pos:end].split('\n', 1)[0]
        return before + after + '\n' + ' ' * len(before) + '^\n'

    def match_examples(self, parse_fn, examples):
        """ Given a parser instance and a dictionary mapping some label with
            some malformed syntax examples, it'll return the label for the
            example that bests matches the current error.
        """
        assert self.state is not None, "Not supported for this exception"

        candidate = None
        for label, example in examples.items():
            assert not isinstance(example, STRING_TYPE)

            for malformed in example:
                try:
                    parse_fn(malformed)
                except UnexpectedInput as ut:
                    if ut.state == self.state:
                        try:
                            if ut.token == self.token:  # Try exact match first
                                return label
                        except AttributeError:
                            pass
                        if not candidate:
                            candidate = label

        return candidate


class UnexpectedCharacters(LexError, UnexpectedInput):
    def __init__(self, seq, lex_pos, line, column, allowed=None, considered_tokens=None, state=None):
        message = "No terminal defined for %s at line %d col %d" % (repr(seq[lex_pos]), line, column)

        self.line = line
        self.column = column
        self.allowed = allowed
        self.considered_tokens = considered_tokens
        self.pos_in_stream = lex_pos
        self.state = state

        message += '\n' + self.get_context(seq)
        if allowed:
            message += '\nExpecting: %s\n' % allowed

        super(UnexpectedCharacters, self).__init__(message)



class UnexpectedToken(ParseError, UnexpectedInput):
    def __init__(self, token, expected, considered_rules=None, state=None):
        self.token = token
        self.expected = expected     # XXX str shouldn't necessary
        self.line = getattr(token, 'line', '?')
        self.column = getattr(token, 'column', '?')
        self.considered_rules = considered_rules
        self.state = state
        self.pos_in_stream = getattr(token, 'pos_in_stream', None)

        message = ("Unexpected token %r at line %s, column %s.\n"
                   "Expected one of: \n\t* %s\n"
                   % (token, self.line, self.column, '\n\t* '.join(self.expected)))

        super(UnexpectedToken, self).__init__(message)


try:
    STRING_TYPE = basestring
except NameError:   # Python 3
    STRING_TYPE = str


import types
from functools import wraps, partial
from contextlib import contextmanager

Str = type(u'')

def smart_decorator(f, create_decorator):
    if isinstance(f, types.FunctionType):
        return wraps(f)(create_decorator(f, True))

    elif isinstance(f, (type, types.BuiltinFunctionType)):
        return wraps(f)(create_decorator(f, False))

    elif isinstance(f, types.MethodType):
        return wraps(f)(create_decorator(f.__func__, True))

    elif isinstance(f, partial):
        # wraps does not work for partials in 2.7: https://bugs.python.org/issue3445
        return create_decorator(f.__func__, True)

    else:
        return create_decorator(f.__func__.__call__, True)



class Meta:
    pass

class Tree(object):
    def __init__(self, data, children, meta=None):
        self.data = data
        self.children = children
        self._meta = meta

    @property
    def meta(self):
        if self._meta is None:
            self._meta = Meta()
        return self._meta

    def __repr__(self):
        return 'Tree(%s, %s)' % (self.data, self.children)

    def _pretty_label(self):
        return self.data

    def _pretty(self, level, indent_str):
        if len(self.children) == 1 and not isinstance(self.children[0], Tree):
            return [ indent_str*level, self._pretty_label(), '\t', '%s' % (self.children[0],), '\n']

        l = [ indent_str*level, self._pretty_label(), '\n' ]
        for n in self.children:
            if isinstance(n, Tree):
                l += n._pretty(level+1, indent_str)
            else:
                l += [ indent_str*(level+1), '%s' % (n,), '\n' ]

        return l

    def pretty(self, indent_str='  '):
        return ''.join(self._pretty(0, indent_str))
    def __eq__(self, other):
        try:
            return self.data == other.data and self.children == other.children
        except AttributeError:
            return False

    def __ne__(self, other):
        return not (self == other)

    def __hash__(self):
        return hash((self.data, tuple(self.children)))

from inspect import getmembers, getmro

class Discard(Exception):
    pass

# Transformers

class Transformer:
    """Visits the tree recursively, starting with the leaves and finally the root (bottom-up)

    Calls its methods (provided by user via inheritance) according to tree.data
    The returned value replaces the old one in the structure.

    Can be used to implement map or reduce.
    """

    def _call_userfunc(self, tree, new_children=None):
        # Assumes tree is already transformed
        children = new_children if new_children is not None else tree.children
        try:
            f = getattr(self, tree.data)
        except AttributeError:
            return self.__default__(tree.data, children, tree.meta)
        else:
            if getattr(f, 'meta', False):
                return f(children, tree.meta)
            elif getattr(f, 'inline', False):
                return f(*children)
            elif getattr(f, 'whole_tree', False):
                if new_children is not None:
                    raise NotImplementedError("Doesn't work with the base Transformer class")
                return f(tree)
            else:
                return f(children)

    def _transform_children(self, children):
        for c in children:
            try:
                yield self._transform_tree(c) if isinstance(c, Tree) else c
            except Discard:
                pass

    def _transform_tree(self, tree):
        children = list(self._transform_children(tree.children))
        return self._call_userfunc(tree, children)

    def transform(self, tree):
        return self._transform_tree(tree)

    def __mul__(self, other):
        return TransformerChain(self, other)

    def __default__(self, data, children, meta):
        "Default operation on tree (for override)"
        return Tree(data, children, meta)

    @classmethod
    def _apply_decorator(cls, decorator, **kwargs):
        mro = getmro(cls)
        assert mro[0] is cls
        libmembers = {name for _cls in mro[1:] for name, _ in getmembers(_cls)}
        for name, value in getmembers(cls):
            if name.startswith('_') or name in libmembers:
                continue

            setattr(cls, name, decorator(value, **kwargs))
        return cls


class InlineTransformer(Transformer):   # XXX Deprecated
    def _call_userfunc(self, tree, new_children=None):
        # Assumes tree is already transformed
        children = new_children if new_children is not None else tree.children
        try:
            f = getattr(self, tree.data)
        except AttributeError:
            return self.__default__(tree.data, children, tree.meta)
        else:
            return f(*children)


class TransformerChain(object):
    def __init__(self, *transformers):
        self.transformers = transformers

    def transform(self, tree):
        for t in self.transformers:
            tree = t.transform(tree)
        return tree

    def __mul__(self, other):
        return TransformerChain(*self.transformers + (other,))


class Transformer_InPlace(Transformer):
    "Non-recursive. Changes the tree in-place instead of returning new instances"
    def _transform_tree(self, tree):           # Cancel recursion
        return self._call_userfunc(tree)

    def transform(self, tree):
        for subtree in tree.iter_subtrees():
            subtree.children = list(self._transform_children(subtree.children))

        return self._transform_tree(tree)


class Transformer_InPlaceRecursive(Transformer):
    "Recursive. Changes the tree in-place instead of returning new instances"
    def _transform_tree(self, tree):
        tree.children = list(self._transform_children(tree.children))
        return self._call_userfunc(tree)



# Visitors

class VisitorBase:
    def _call_userfunc(self, tree):
        return getattr(self, tree.data, self.__default__)(tree)

    def __default__(self, tree):
        "Default operation on tree (for override)"
        return tree


class Visitor(VisitorBase):
    """Bottom-up visitor, non-recursive

    Visits the tree, starting with the leaves and finally the root (bottom-up)
    Calls its methods (provided by user via inheritance) according to tree.data
    """


    def visit(self, tree):
        for subtree in tree.iter_subtrees():
            self._call_userfunc(subtree)
        return tree

class Visitor_Recursive(VisitorBase):
    """Bottom-up visitor, recursive

    Visits the tree, starting with the leaves and finally the root (bottom-up)
    Calls its methods (provided by user via inheritance) according to tree.data
    """

    def visit(self, tree):
        for child in tree.children:
            if isinstance(child, Tree):
                self.visit(child)

        f = getattr(self, tree.data, self.__default__)
        f(tree)
        return tree



def visit_children_decor(func):
    "See Interpreter"
    @wraps(func)
    def inner(cls, tree):
        values = cls.visit_children(tree)
        return func(cls, values)
    return inner


class Interpreter:
    """Top-down visitor, recursive

    Visits the tree, starting with the root and finally the leaves (top-down)
    Calls its methods (provided by user via inheritance) according to tree.data

    Unlike Transformer and Visitor, the Interpreter doesn't automatically visit its sub-branches.
    The user has to explicitly call visit_children, or use the @visit_children_decor
    """
    def visit(self, tree):
        return getattr(self, tree.data)(tree)

    def visit_children(self, tree):
        return [self.visit(child) if isinstance(child, Tree) else child
                for child in tree.children]

    def __getattr__(self, name):
        return self.__default__

    def __default__(self, tree):
        return self.visit_children(tree)




# Decorators

def _apply_decorator(obj, decorator, **kwargs):
    try:
        _apply = obj._apply_decorator
    except AttributeError:
        return decorator(obj, **kwargs)
    else:
        return _apply(decorator, **kwargs)



def _inline_args__func(func):
    @wraps(func)
    def create_decorator(_f, with_self):
        if with_self:
            def f(self, children):
                return _f(self, *children)
        else:
            def f(self, children):
                return _f(*children)
        return f

    return smart_decorator(func, create_decorator)


def inline_args(obj):   # XXX Deprecated
    return _apply_decorator(obj, _inline_args__func)



def _visitor_args_func_dec(func, inline=False, meta=False, whole_tree=False):
    assert [whole_tree, meta, inline].count(True) <= 1
    def create_decorator(_f, with_self):
        if with_self:
            def f(self, *args, **kwargs):
                return _f(self, *args, **kwargs)
        else:
            def f(self, *args, **kwargs):
                return _f(*args, **kwargs)
        return f

    f = smart_decorator(func, create_decorator)
    f.inline = inline
    f.meta = meta
    f.whole_tree = whole_tree
    return f

def v_args(inline=False, meta=False, tree=False):
    "A convenience decorator factory, for modifying the behavior of user-supplied visitor methods"
    if [tree, meta, inline].count(True) > 1:
        raise ValueError("Visitor functions can either accept tree, or meta, or be inlined. These cannot be combined.")
    def _visitor_args_dec(obj):
        return _apply_decorator(obj, _visitor_args_func_dec, inline=inline, meta=meta, whole_tree=tree)
    return _visitor_args_dec



class Indenter:
    def __init__(self):
        self.paren_level = 0
        self.indent_level = [0]

    def handle_NL(self, token):
        if self.paren_level > 0:
            return

        yield token

        indent_str = token.rsplit('\n', 1)[1] # Tabs and spaces
        indent = indent_str.count(' ') + indent_str.count('\t') * self.tab_len

        if indent > self.indent_level[-1]:
            self.indent_level.append(indent)
            yield Token.new_borrow_pos(self.INDENT_type, indent_str, token)
        else:
            while indent < self.indent_level[-1]:
                self.indent_level.pop()
                yield Token.new_borrow_pos(self.DEDENT_type, indent_str, token)

            assert indent == self.indent_level[-1], '%s != %s' % (indent, self.indent_level[-1])

    def process(self, stream):
        for token in stream:
            if token.type == self.NL_type:
                for t in self.handle_NL(token):
                    yield t
            else:
                yield token

            if token.type in self.OPEN_PAREN_types:
                self.paren_level += 1
            elif token.type in self.CLOSE_PAREN_types:
                self.paren_level -= 1
                assert self.paren_level >= 0

        while len(self.indent_level) > 1:
            self.indent_level.pop()
            yield Token(self.DEDENT_type, '')

        assert self.indent_level == [0], self.indent_level

    # XXX Hack for ContextualLexer. Maybe there's a more elegant solution?
    @property
    def always_accept(self):
        return (self.NL_type,)


parser_errors = []

class Token(Str):
    __slots__ = ('type', 'pos_in_stream', 'value', 'line', 'column', 'end_line', 'end_column')

    def __new__(cls, type_, value, pos_in_stream=None, line=None, column=None):
        self = super(Token, cls).__new__(cls, value)
        self.type = type_
        self.pos_in_stream = pos_in_stream
        self.value = value
        self.line = line
        self.column = column
        self.end_line = None
        self.end_column = None
        return self

    @classmethod
    def new_borrow_pos(cls, type_, value, borrow_t):
        return cls(type_, value, borrow_t.pos_in_stream, line=borrow_t.line, column=borrow_t.column)

    def __reduce__(self):
        return (self.__class__, (self.type, self.value, self.pos_in_stream, self.line, self.column, ))

    def __repr__(self):
        return 'Token(%s, %r)' % (self.type, self.value)

    def __deepcopy__(self, memo):
        return Token(self.type, self.value, self.pos_in_stream, self.line, self.column)

    def __eq__(self, other):
        if isinstance(other, Token) and self.type != other.type:
            return False

        return Str.__eq__(self, other)

    __hash__ = Str.__hash__


class LineCounter:
    def __init__(self):
        self.newline_char = '\n'
        self.char_pos = 0
        self.line = 1
        self.column = 1
        self.line_start_pos = 0

    def feed(self, token, test_newline=True):
        """Consume a token and calculate the new line & column.

        As an optional optimization, set test_newline=False is token doesn't contain a newline.
        """
        if test_newline:
            newlines = token.count(self.newline_char)
            if newlines:
                self.line += newlines
                self.line_start_pos = self.char_pos + token.rindex(self.newline_char) + 1

        self.char_pos += len(token)
        self.column = self.char_pos - self.line_start_pos + 1

class _Lex:
    "Built to serve both Lexer and ContextualLexer"
    def __init__(self, lexer, state=None):
        self.lexer = lexer
        self.state = state

    def lex(self, stream, newline_types, ignore_types):
        newline_types = list(newline_types)
        ignore_types = list(ignore_types)
        line_ctr = LineCounter()
        char_error_offset = 0
        line_error_offset = 0
        column_error_offset = 0
        last_error_line = -1
        original_stream = stream

        t = None
        while True:
            lexer = self.lexer
            for mre, type_from_index in lexer.mres:
                log(2, 'stream %s, mre %s', stream[0:50], mre)
                m = mre.match(stream, line_ctr.char_pos)
                if m:
                    value = m.group(0)
                    type_ = type_from_index[m.lastindex]
                    if type_ not in ignore_types:
                        t = Token(type_, value, line_ctr.char_pos, line_ctr.line, line_ctr.column)
                        if t.type in lexer.callback:
                            t = lexer.callback[t.type](t)
                        yield t
                    else:
                        if type_ in lexer.callback:
                            t = Token(type_, value, line_ctr.char_pos, line_ctr.line, line_ctr.column)
                            lexer.callback[type_](t)

                    line_ctr.feed(value, type_ in newline_types)
                    if t:
                        t.end_line = line_ctr.line
                        t.end_column = line_ctr.column

                    break
            else:
                if line_ctr.char_pos < len(stream):
                    if last_error_line != line_ctr.line:
                        parser_errors[-1].append( UnexpectedCharacters( original_stream,
                                line_ctr.char_pos + char_error_offset,
                                line_ctr.line + line_error_offset,
                                line_ctr.column + column_error_offset,
                                state=self.state
                            )
                        )

                    log(2, 'parser_errors: %s', parser_errors)
                    char_error_offset += 1
                    column_error_offset += 1

                    if stream[line_ctr.char_pos] == '\n':
                        line_error_offset += 1
                        column_error_offset = 0

                    last_error_line = line_ctr.line
                    stream = stream[0:line_ctr.char_pos] + stream[line_ctr.char_pos+1:]

                else:
                    break

        if t:
            t.end_line = line_ctr.line
            t.end_column = line_ctr.column

class UnlessCallback:
    def __init__(self, mres):
        self.mres = mres

    def __call__(self, t):
        for mre, type_from_index in self.mres:
            m = mre.match(t.value)
            if m:
                t.type = type_from_index[m.lastindex]
                break
        return t


from functools import partial, wraps


class ExpandSingleChild:
    def __init__(self, node_builder):
        self.node_builder = node_builder

    def __call__(self, children):
        if len(children) == 1:
            return children[0]
        else:
            return self.node_builder(children)


class PropagatePositions:
    def __init__(self, node_builder):
        self.node_builder = node_builder

    def __call__(self, children):
        res = self.node_builder(children)
        res.meta.empty = True

        if children and isinstance(res, Tree):
            for c in children:
                if isinstance(c, Tree) and c.children and not c.meta.empty:
                    res.meta.line = c.meta.line
                    res.meta.column = c.meta.column
                    res.meta.start_pos = c.meta.start_pos
                    res.meta.empty = False
                    break
                elif isinstance(c, Token):
                    res.meta.line = c.line
                    res.meta.column = c.column
                    res.meta.start_pos = c.pos_in_stream
                    res.meta.empty = False
                    break

            for c in reversed(children):
                if isinstance(c, Tree) and c.children and not c.meta.empty:
                    res.meta.end_line = c.meta.end_line
                    res.meta.end_column = c.meta.end_column
                    res.meta.end_pos = c.meta.end_pos
                    res.meta.empty = False
                    break
                elif isinstance(c, Token):
                    res.meta.end_line = c.end_line
                    res.meta.end_column = c.end_column
                    res.meta.end_pos = c.pos_in_stream + len(c.value)
                    res.meta.empty = False
                    break

        return res


class ChildFilter:
    def __init__(self, to_include, node_builder):
        self.node_builder = node_builder
        self.to_include = to_include

    def __call__(self, children):
        filtered = []
        for i, to_expand in self.to_include:
            if to_expand:
                filtered += children[i].children
            else:
                filtered.append(children[i])

        return self.node_builder(filtered)

class ChildFilterLALR(ChildFilter):
    "Optimized childfilter for LALR (assumes no duplication in parse tree, so it's safe to change it)"

    def __call__(self, children):
        filtered = []
        for i, to_expand in self.to_include:
            if to_expand:
                if filtered:
                    filtered += children[i].children
                else:   # Optimize for left-recursion
                    filtered = children[i].children
            else:
                filtered.append(children[i])

        return self.node_builder(filtered)

def _should_expand(sym):
    return not sym.is_term and sym.name.startswith('_')

def maybe_create_child_filter(expansion, keep_all_tokens, ambiguous):
    to_include = [(i, _should_expand(sym)) for i, sym in enumerate(expansion)
                  if keep_all_tokens or not (sym.is_term and sym.filter_out)]

    if len(to_include) < len(expansion) or any(to_expand for i, to_expand in to_include):
        return partial(ChildFilter if ambiguous else ChildFilterLALR, to_include)


class Callback(object):
    pass


def ptb_inline_args(func):
    @wraps(func)
    def f(children):
        return func(*children)
    return f



class ParseTreeBuilder:
    def __init__(self, rules, tree_class, propagate_positions=False, keep_all_tokens=False, ambiguous=False):
        self.tree_class = tree_class
        self.propagate_positions = propagate_positions
        self.always_keep_all_tokens = keep_all_tokens
        self.ambiguous = ambiguous

        self.rule_builders = list(self._init_builders(rules))

        self.user_aliases = {}

    def _init_builders(self, rules):
        for rule in rules:
            options = rule.options
            keep_all_tokens = self.always_keep_all_tokens or (options.keep_all_tokens if options else False)
            expand_single_child = options.expand1 if options else False

            wrapper_chain = filter(None, [
                (expand_single_child and not rule.alias) and ExpandSingleChild,
                maybe_create_child_filter(rule.expansion, keep_all_tokens, self.ambiguous),
                self.propagate_positions and PropagatePositions,
            ])

            yield rule, wrapper_chain


    def create_callback(self, transformer=None):
        callback = Callback()

        i = 0
        for rule, wrapper_chain in self.rule_builders:
            internal_callback_name = '_cb%d_%s' % (i, rule.origin)
            i += 1

            user_callback_name = rule.alias or rule.origin.name
            try:
                f = getattr(transformer, user_callback_name)
                assert not getattr(f, 'meta', False), "Meta args not supported for internal transformer"
                # XXX InlineTransformer is deprecated!
                if getattr(f, 'inline', False) or isinstance(transformer, InlineTransformer):
                    f = ptb_inline_args(f)
            except AttributeError:
                f = partial(self.tree_class, user_callback_name)

            self.user_aliases[rule] = rule.alias
            rule.alias = internal_callback_name

            for w in wrapper_chain:
                f = w(f)

            if hasattr(callback, internal_callback_name):
                raise GrammarError("Rule '%s' already exists" % (rule,))
            setattr(callback, internal_callback_name, f)

        return callback



class _Parser:
    def __init__(self, parse_table, callbacks):
        self.states = parse_table.states
        self.start_state = parse_table.start_state
        self.end_state = parse_table.end_state
        self.callbacks = callbacks
        # TODO Verify whether `parser_errors` can be a member class atribute.
        # It depends on whether this is an member class object or static.
        # self.parser_errors = []

    def parse(self, seq, set_state=None):
        # stack of error contexts allowing this function to be reentrant
        # parser_errors = self.parser_errors
        parser_errors.append([])

        token = None
        stream = iter(seq)
        states = self.states

        state_stack = [self.start_state]
        value_stack = []

        if set_state: set_state(self.start_state)

        def get_action(key):
            state = state_stack[-1]
            try:
                return states[state][key]
            except KeyError:
                expected = [s for s in states[state].keys() if s.isupper()]

                # TODO filter out rules from expected
                parser_errors[-1].append(UnexpectedToken(token, expected, state=state))
                log(2, 'parser_errors %s', parser_errors)

                # Just take the first expected key
                for s in states[state].keys():
                    if s.isupper():
                        log( 2, 'For %s and %s, returning key %s - %s', state, key, s, states[state][s] )
                        return states[state][s]

        def reduce(rule):
            size = len(rule.expansion)
            if size:
                s = value_stack[-size:]
                del state_stack[-size:]
                del value_stack[-size:]
            else:
                s = []

            value = self.callbacks[rule](s)

            _action, new_state = get_action(rule.origin.name)
            assert _action is Shift
            state_stack.append(new_state)
            value_stack.append(value)

        def print_partial_tree():
            delimiter = '\n--------------\n'
            for item in value_stack:
                if isinstance(item, Tree):
                    sys.stderr.write('\npartial tree:%s%s\n' % (delimiter, item.pretty()))
                else:
                    sys.stderr.write('\nloose token:%s%s\n' % (delimiter, repr(item)))

            sys.stderr.write(delimiter)

        def raise_parsing_errors():
            log(2, 'parser_errors: %s', parser_errors)
            if parser_errors[-1]:
                error_messages = []
                for index, exception in enumerate(parser_errors[-1]):
                    # Comment out this if to show the duplicated error messages removed
                    if not index or index > 0 and exception != parser_errors[-1][index-1]:
                        if type(exception) is UnexpectedCharacters:
                            error_messages.append('\nLexer error: %s' % exception)
                        elif type(exception) is UnexpectedToken:
                            error_messages.append('\nParser error: %s' % exception)
                print_partial_tree()
                raise SyntaxError(''.join( error_messages ))

        try:
            # Main LALR-parser loop
            log( 2, '' ); index = -1
            for token in stream:
                index += 1; x = token; log( 2, repr("[@%s,%s:%s='%s'<%s>,%s:%s]"), index, x.pos_in_stream, x.pos_in_stream+len(x.value)-1, x.value, x.type, x.line, x.column )
                while True:
                    log( 2, 'token %s %s', type(token), token)
                    action, arg = get_action(token.type)
                    if arg == self.end_state:
                        break
                    if action is Shift:
                        state_stack.append(arg)
                        value_stack.append(token)
                        if set_state: set_state(arg)
                        break # next token
                    else:
                        reduce(arg)

            raise_parsing_errors()
            token = Token.new_borrow_pos('<EOF>', token, token) if token else Token('<EOF>', '', 0, 1, 1)

            while True:
                _action, arg = get_action('$END')
                if _action is Shift:
                    if arg != self.end_state:
                        raise_parsing_errors()
                        assert arg == self.end_state
                    val ,= value_stack
                    return val
                else:
                    reduce(arg)

            raise_parsing_errors()

        finally:
            # unstack the current erro context on finish
            parser_errors.pop()


class Symbol(object):
    is_term = NotImplemented

    def __init__(self, name):
        self.name = name

    def __eq__(self, other):
        assert isinstance(other, Symbol), other
        return self.is_term == other.is_term and self.name == other.name

    def __ne__(self, other):
        return not (self == other)

    def __hash__(self):
        return hash(self.name)

    def __repr__(self):
        return '%s(%r)' % (type(self).__name__, self.name)

    fullrepr = property(__repr__)

class Terminal(Symbol):
    is_term = True

    def __init__(self, name, filter_out=False):
        self.name = name
        self.filter_out = filter_out

    @property
    def fullrepr(self):
        return '%s(%r, %r)' % (type(self).__name__, self.name, self.filter_out)


class NonTerminal(Symbol):
    is_term = False

class Rule(object):
    """
        origin : a symbol
        expansion : a list of symbols
    """
    def __init__(self, origin, expansion, alias=None, options=None):
        self.origin = origin
        self.expansion = expansion
        self.alias = alias
        self.options = options

    def __str__(self):
        return '<%s : %s>' % (self.origin, ' '.join(map(str,self.expansion)))

    def __repr__(self):
        return 'Rule(%r, %r, %r, %r)' % (self.origin, self.expansion, self.alias, self.options)


class RuleOptions:
    def __init__(self, keep_all_tokens=False, expand1=False, priority=None):
        self.keep_all_tokens = keep_all_tokens
        self.expand1 = expand1
        self.priority = priority

    def __repr__(self):
        return 'RuleOptions(%r, %r, %r)' % (
            self.keep_all_tokens,
            self.expand1,
            self.priority,
        )

Shift = 0
Reduce = 1
import re
class LexerRegexps: pass
NEWLINE_TYPES = ['SINGLE_LINE_COMMENT', '_NEWLINE', '__ANON_0', '__ANON_2']
IGNORE_TYPES = ['SPACES', 'MULTI_LINE_COMMENT', 'SINGLE_LINE_COMMENT']
LEXERS = {}
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<SCOPE>scope)|(?P<NAME>name)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'SCOPE',
   9: 'NAME'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[0] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'CONTEXTS'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[1] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<SCOPE>scope)|(?P<NAME>name)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'SCOPE',
   7: 'NAME'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[2] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[3] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)|(?P<SCOPE>scope)|(?P<NAME>name)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'CONTEXTS',
   9: 'SCOPE',
   10: 'NAME'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[4] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[5] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[6] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[7] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[8] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'CONTEXTS'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[9] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[10] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[11] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'CONTEXTS'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[12] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<__ANON_2>(\\\\{|\\\\}|[^\n'
  '{}])+)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT',
   3: '__ANON_2',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[13] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[14] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[15] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[16] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<__ANON_2>(\\\\{|\\\\}|[^\n'
  '{}])+)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT',
   3: '__ANON_2',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[17] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)|(?P<SCOPE>scope)|(?P<NAME>name)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'CONTEXTS',
   9: 'SCOPE',
   10: 'NAME'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[18] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)|(?P<SCOPE>scope)|(?P<NAME>name)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'CONTEXTS',
   9: 'SCOPE',
   10: 'NAME'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[19] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[20] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[21] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[22] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[23] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[24] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<LBRACE>\\{)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'LBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[25] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[26] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'CONTEXTS'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[27] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<LBRACE>\\{)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'LBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[28] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[29] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)|(?P<SCOPE>scope)|(?P<NAME>name)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'CONTEXTS',
   9: 'SCOPE',
   10: 'NAME'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[30] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<CONTEXTS>contexts)|(?P<SCOPE>scope)|(?P<NAME>name)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: 'CONTEXTS',
   9: 'SCOPE',
   10: 'NAME'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[31] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[32] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[33] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[34] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[35] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[36] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[37] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<LBRACE>\\{)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'LBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[38] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[39] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[40] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[41] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[42] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[43] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[44] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[45] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[46] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[47] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[48] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[49] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[50] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[51] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_1>meta_scope)|(?P<INCLUDE>include)|(?P<MATCH>match)|(?P<PUSH>push)|(?P<POP>pop)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: '__ANON_1',
   7: 'INCLUDE',
   8: 'MATCH',
   9: 'PUSH',
   10: 'POP'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[52] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[53] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[54] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[55] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[56] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[57] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[58] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[59] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[60] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[61] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[62] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[63] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_1>meta_scope)|(?P<INCLUDE>include)|(?P<MATCH>match)|(?P<PUSH>push)|(?P<POP>pop)|(?P<RBRACE>\\})',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: '__ANON_1',
   7: 'INCLUDE',
   8: 'MATCH',
   9: 'PUSH',
   10: 'POP',
   11: 'RBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[64] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[65] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[66] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[67] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[68] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[69] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[70] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[71] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[72] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[73] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[74] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<__ANON_2>(\\\\{|\\\\}|[^\n'
  '{}])+)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT',
   3: '__ANON_2',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[75] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<LBRACE>\\{)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'LBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[76] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<__ANON_2>(\\\\{|\\\\}|[^\n'
  '{}])+)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT',
   3: '__ANON_2',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[77] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_0>[^:\n'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES',
   8: '__ANON_0'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[78] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[79] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<__ANON_2>(\\\\{|\\\\}|[^\n'
  '{}])+)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT',
   3: '__ANON_2',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[80] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_1>meta_scope)|(?P<INCLUDE>include)|(?P<MATCH>match)|(?P<PUSH>push)|(?P<POP>pop)|(?P<RBRACE>\\})',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: '__ANON_1',
   7: 'INCLUDE',
   8: 'MATCH',
   9: 'PUSH',
   10: 'POP',
   11: 'RBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[81] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<__ANON_2>(\\\\{|\\\\}|[^\n'
  '{}])+)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT',
   3: '__ANON_2',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[82] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[83] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[84] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[85] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[86] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_1>meta_scope)|(?P<INCLUDE>include)|(?P<MATCH>match)|(?P<PUSH>push)|(?P<POP>pop)|(?P<RBRACE>\\})',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: '__ANON_1',
   7: 'INCLUDE',
   8: 'MATCH',
   9: 'PUSH',
   10: 'POP',
   11: 'RBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[87] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[88] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<LBRACE>\\{)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'LBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[89] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[90] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_1>meta_scope)|(?P<CAPTURES>captures)|(?P<INCLUDE>include)|(?P<MATCH>match)|(?P<SCOPE>scope)|(?P<PUSH>push)|(?P<POP>pop)|(?P<RBRACE>\\})',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: '__ANON_1',
   7: 'CAPTURES',
   8: 'INCLUDE',
   9: 'MATCH',
   10: 'SCOPE',
   11: 'PUSH',
   12: 'POP',
   13: 'RBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[91] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[92] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[93] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[94] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[95] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[96] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[97] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT', 3: 'SINGLE_LINE_COMMENT', 5: 'SPACES', 6: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[98] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[99] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<__ANON_1>meta_scope)|(?P<CAPTURES>captures)|(?P<INCLUDE>include)|(?P<MATCH>match)|(?P<SCOPE>scope)|(?P<PUSH>push)|(?P<POP>pop)|(?P<RBRACE>\\})',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: '__ANON_1',
   7: 'CAPTURES',
   8: 'INCLUDE',
   9: 'MATCH',
   10: 'SCOPE',
   11: 'PUSH',
   12: 'POP',
   13: 'RBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[100] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<LBRACE>\\{)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'LBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[101] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<__ANON_2>(\\\\{|\\\\}|[^\n'
  '{}])+)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT',
   3: '__ANON_2',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[102] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[103] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[104] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[105] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[106] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[107] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<INTEGER>[0-9])',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'INTEGER'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[108] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<INTEGER>[0-9])|(?P<RBRACE>\\})',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'INTEGER',
   7: 'RBRACE'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[109] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[110] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<INTEGER>[0-9])|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'INTEGER',
   7: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[111] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<INTEGER>[0-9])|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'INTEGER',
   7: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[112] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[113] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[114] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)|(?P<INTEGER>[0-9])|(?P<COLON>\\:)',
  {1: 'MULTI_LINE_COMMENT',
   3: 'SINGLE_LINE_COMMENT',
   5: 'SPACES',
   6: 'INTEGER',
   7: 'COLON'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[115] = (lexer_regexps)
MRES = (
[('(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<__ANON_2>(\\\\{|\\\\}|[^\n'
  '{}])+)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: 'MULTI_LINE_COMMENT',
   3: '__ANON_2',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[116] = (lexer_regexps)
MRES = (
[('(?P<_NEWLINE>(?:(?:\r'
  '?\n'
  '[\t ]*|(\\#|\\/\\/)[^\n'
  ']*))+)|(?P<MULTI_LINE_COMMENT>\\/\\*([\\s\\S]*?)\\*\\/)|(?P<SINGLE_LINE_COMMENT>(\\#|\\/\\/)[^\n'
  ']*)|(?P<SPACES>[\t \x0c'
  ']+)',
  {1: '_NEWLINE',
   3: 'MULTI_LINE_COMMENT',
   5: 'SINGLE_LINE_COMMENT',
   7: 'SPACES'})]
)
LEXER_CALLBACK = (
{}
)
lexer_regexps = LexerRegexps()
lexer_regexps.mres = [(re.compile(p), d) for p, d in MRES]
lexer_regexps.callback = {n: UnlessCallback([(re.compile(p), d) for p, d in mres])
                          for n, mres in LEXER_CALLBACK.items()}
LEXERS[117] = (lexer_regexps)
class ContextualLexer:
    def __init__(self):
        self.lexers = LEXERS
        self.set_parser_state(None)
    def set_parser_state(self, state):
        self.parser_state = state
    def lex(self, stream):
        newline_types = NEWLINE_TYPES
        ignore_types = IGNORE_TYPES
        lexers = LEXERS
        l = _Lex(lexers[self.parser_state], self.parser_state)
        for x in l.lex(stream, newline_types, ignore_types):
            yield x
            l.lexer = lexers[self.parser_state]
            l.state = self.parser_state
CON_LEXER = ContextualLexer()
def lex(stream):
    return CON_LEXER.lex(stream)
RULES = {
  0: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), NonTerminal('__anon_star_0')], None, RuleOptions(False, False, None)),
  1: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  2: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), NonTerminal('__anon_star_0')], None, RuleOptions(False, False, None)),
  3: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  4: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  5: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), NonTerminal('__anon_star_0'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  6: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules')], None, RuleOptions(False, False, None)),
  7: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), NonTerminal('__anon_star_0'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  8: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  9: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  10: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), NonTerminal('__anon_star_0')], None, RuleOptions(False, False, None)),
  11: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), NonTerminal('__anon_star_0'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  12: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), NonTerminal('__anon_star_0'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  13: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), NonTerminal('__anon_star_0')], None, RuleOptions(False, False, None)),
  14: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), NonTerminal('language_construct_rules')], None, RuleOptions(False, False, None)),
  15: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), NonTerminal('__anon_star_0')], None, RuleOptions(False, False, None)),
  16: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules')], None, RuleOptions(False, False, None)),
  17: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), NonTerminal('__anon_star_0')], None, RuleOptions(False, False, None)),
  18: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), NonTerminal('__anon_star_0'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  19: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  20: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  21: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  22: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), NonTerminal('__anon_star_0'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  23: Rule(NonTerminal('language_syntax'), [NonTerminal('preamble_statements'), NonTerminal('language_construct_rules')], None, RuleOptions(False, False, None)),
  24: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), NonTerminal('__anon_star_0')], None, RuleOptions(False, False, None)),
  25: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), NonTerminal('__anon_star_0')], None, RuleOptions(False, False, None)),
  26: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), Terminal('_NEWLINE', True), NonTerminal('language_construct_rules'), NonTerminal('__anon_star_0'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  27: Rule(NonTerminal('language_syntax'), [Terminal('_NEWLINE', True), NonTerminal('preamble_statements'), NonTerminal('language_construct_rules'), Terminal('_NEWLINE', True), NonTerminal('__anon_star_0'), Terminal('_NEWLINE', True)], None, RuleOptions(False, False, None)),
  28: Rule(NonTerminal('preamble_statements'), [NonTerminal('__anon_plus_1')], None, RuleOptions(False, False, None)),
  29: Rule(NonTerminal('language_construct_rules'), [Terminal('CONTEXTS', True), Terminal('COLON', True), NonTerminal('indentation_block')], None, RuleOptions(False, False, None)),
  30: Rule(NonTerminal('miscellaneous_language_rules'), [Terminal('__ANON_0', False), Terminal('COLON', True), NonTerminal('indentation_block')], None, RuleOptions(False, False, None)),
  31: Rule(NonTerminal('target_language_name_statement'), [Terminal('NAME', True), Terminal('COLON', True), NonTerminal('free_input_string')], None, RuleOptions(False, False, None)),
  32: Rule(NonTerminal('master_scope_name_statement'), [Terminal('SCOPE', True), Terminal('COLON', True), NonTerminal('free_input_string')], None, RuleOptions(False, False, None)),
  33: Rule(NonTerminal('indentation_block'), [Terminal('LBRACE', True), Terminal('_NEWLINE', True), NonTerminal('__anon_plus_2'), Terminal('RBRACE', True)], None, RuleOptions(False, False, None)),
  34: Rule(NonTerminal('statements_list'), [NonTerminal('include_statement')], None, RuleOptions(False, False, None)),
  35: Rule(NonTerminal('statements_list'), [NonTerminal('push_statement')], None, RuleOptions(False, False, None)),
  36: Rule(NonTerminal('statements_list'), [NonTerminal('match_statement')], None, RuleOptions(False, False, None)),
  37: Rule(NonTerminal('statements_list'), [NonTerminal('pop_statement')], None, RuleOptions(False, False, None)),
  38: Rule(NonTerminal('statements_list'), [NonTerminal('meta_scope_statement')], None, RuleOptions(False, False, None)),
  39: Rule(NonTerminal('push_statement'), [Terminal('PUSH', True), Terminal('COLON', True), NonTerminal('indentation_block')], None, RuleOptions(False, False, None)),
  40: Rule(NonTerminal('include_statement'), [Terminal('INCLUDE', True), Terminal('COLON', True), NonTerminal('free_input_string')], None, RuleOptions(False, False, None)),
  41: Rule(NonTerminal('match_statements'), [NonTerminal('scope_name')], None, RuleOptions(False, False, None)),
  42: Rule(NonTerminal('match_statements'), [NonTerminal('statements_list')], None, RuleOptions(False, False, None)),
  43: Rule(NonTerminal('match_statements'), [NonTerminal('capturing_block')], None, RuleOptions(False, False, None)),
  44: Rule(NonTerminal('match_statement'), [Terminal('MATCH', True), Terminal('COLON', True), NonTerminal('free_input_string'), Terminal('LBRACE', True), NonTerminal('__anon_star_3'), Terminal('_NEWLINE', True), Terminal('RBRACE', True)], None, RuleOptions(False, False, None)),
  45: Rule(NonTerminal('match_statement'), [Terminal('MATCH', True), Terminal('COLON', True), NonTerminal('free_input_string'), Terminal('LBRACE', True), Terminal('_NEWLINE', True), Terminal('RBRACE', True)], None, RuleOptions(False, False, None)),
  46: Rule(NonTerminal('scope_name'), [Terminal('SCOPE', True), Terminal('COLON', True), NonTerminal('free_input_string')], None, RuleOptions(False, False, None)),
  47: Rule(NonTerminal('capturing_block'), [Terminal('CAPTURES', True), Terminal('COLON', True), Terminal('LBRACE', True), NonTerminal('__anon_plus_4'), Terminal('_NEWLINE', True), Terminal('RBRACE', True)], None, RuleOptions(False, False, None)),
  48: Rule(NonTerminal('capturing_lines'), [NonTerminal('__anon_plus_5'), Terminal('COLON', True), NonTerminal('free_input_string')], None, RuleOptions(False, False, None)),
  49: Rule(NonTerminal('pop_statement'), [Terminal('POP', True), Terminal('COLON', True), NonTerminal('free_input_string')], None, RuleOptions(False, False, None)),
  50: Rule(NonTerminal('meta_scope_statement'), [Terminal('__ANON_1', True), Terminal('COLON', True), NonTerminal('free_input_string')], None, RuleOptions(False, False, None)),
  51: Rule(NonTerminal('free_input_string'), [Terminal('__ANON_2', False)], None, RuleOptions(False, False, None)),
  52: Rule(NonTerminal('__anon_star_0'), [NonTerminal('__anon_star_0'), NonTerminal('miscellaneous_language_rules'), Terminal('_NEWLINE', True)], None, None),
  53: Rule(NonTerminal('__anon_star_0'), [NonTerminal('miscellaneous_language_rules')], None, None),
  54: Rule(NonTerminal('__anon_star_0'), [NonTerminal('miscellaneous_language_rules'), Terminal('_NEWLINE', True)], None, None),
  55: Rule(NonTerminal('__anon_star_0'), [NonTerminal('__anon_star_0'), NonTerminal('miscellaneous_language_rules')], None, None),
  56: Rule(NonTerminal('__anon_plus_1'), [NonTerminal('target_language_name_statement'), Terminal('_NEWLINE', True)], None, None),
  57: Rule(NonTerminal('__anon_plus_1'), [NonTerminal('master_scope_name_statement'), Terminal('_NEWLINE', True)], None, None),
  58: Rule(NonTerminal('__anon_plus_1'), [NonTerminal('__anon_plus_1'), NonTerminal('target_language_name_statement'), Terminal('_NEWLINE', True)], None, None),
  59: Rule(NonTerminal('__anon_plus_1'), [NonTerminal('__anon_plus_1'), NonTerminal('master_scope_name_statement'), Terminal('_NEWLINE', True)], None, None),
  60: Rule(NonTerminal('__anon_plus_2'), [NonTerminal('statements_list'), Terminal('_NEWLINE', True)], None, None),
  61: Rule(NonTerminal('__anon_plus_2'), [NonTerminal('__anon_plus_2'), NonTerminal('statements_list'), Terminal('_NEWLINE', True)], None, None),
  62: Rule(NonTerminal('__anon_star_3'), [Terminal('_NEWLINE', True), NonTerminal('match_statements')], None, None),
  63: Rule(NonTerminal('__anon_star_3'), [NonTerminal('__anon_star_3'), Terminal('_NEWLINE', True), NonTerminal('match_statements')], None, None),
  64: Rule(NonTerminal('__anon_plus_4'), [NonTerminal('__anon_plus_4'), Terminal('_NEWLINE', True), NonTerminal('capturing_lines')], None, None),
  65: Rule(NonTerminal('__anon_plus_4'), [Terminal('_NEWLINE', True), NonTerminal('capturing_lines')], None, None),
  66: Rule(NonTerminal('__anon_plus_5'), [Terminal('INTEGER', False)], None, None),
  67: Rule(NonTerminal('__anon_plus_5'), [NonTerminal('__anon_plus_5'), Terminal('INTEGER', False)], None, None),
}
parse_tree_builder = ParseTreeBuilder(RULES.values(), Tree)
class ParseTable: pass
parse_table = ParseTable()
STATES = {
  0: {0: (0, 1), 1: (0, 2), 2: (0, 3), 3: (0, 4), 4: (0, 5), 5: (0, 6), 6: (0, 7), 7: (0, 8)},
  1: {1: (0, 9), 8: (0, 10), 9: (0, 11)},
  2: {0: (0, 12), 2: (0, 3), 3: (0, 4), 6: (0, 7), 5: (0, 6), 7: (0, 8)},
  3: {10: (0, 13)},
  4: {9: (1, 28), 1: (1, 28), 6: (0, 14), 5: (0, 6), 7: (0, 15), 2: (0, 3)},
  5: {11: (0, 16)},
  6: {10: (0, 17)},
  7: {1: (0, 18)},
  8: {1: (0, 19)},
  9: {8: (0, 20), 9: (0, 11)},
  10: {11: (1, 23), 12: (0, 21), 13: (0, 22), 14: (0, 23), 1: (0, 24)},
  11: {10: (0, 25)},
  12: {8: (0, 26), 1: (0, 27), 9: (0, 11)},
  13: {15: (0, 28), 16: (0, 29)},
  14: {1: (0, 30)},
  15: {1: (0, 31)},
  16: {},
  17: {15: (0, 28), 16: (0, 32)},
  18: {5: (1, 57), 1: (1, 57), 2: (1, 57), 9: (1, 57)},
  19: {5: (1, 56), 1: (1, 56), 2: (1, 56), 9: (1, 56)},
  20: {11: (1, 6), 13: (0, 33), 12: (0, 21), 1: (0, 34), 14: (0, 23)},
  21: {11: (1, 53), 14: (1, 53), 1: (0, 35)},
  22: {11: (1, 15), 12: (0, 36), 1: (0, 37), 14: (0, 23)},
  23: {10: (0, 38)},
  24: {11: (1, 20), 12: (0, 21), 13: (0, 39), 14: (0, 23), 1: (0, 40)},
  25: {17: (0, 41), 18: (0, 42)},
  26: {11: (1, 14), 1: (0, 43), 12: (0, 21), 13: (0, 44), 14: (0, 23)},
  27: {8: (0, 45), 9: (0, 11)},
  28: {17: (1, 51), 1: (1, 51)},
  29: {1: (1, 32)},
  30: {5: (1, 59), 1: (1, 59), 2: (1, 59), 9: (1, 59)},
  31: {5: (1, 58), 1: (1, 58), 2: (1, 58), 9: (1, 58)},
  32: {1: (1, 31)},
  33: {11: (1, 2), 1: (0, 46), 12: (0, 36), 14: (0, 23)},
  34: {11: (1, 1), 13: (0, 47), 12: (0, 21), 14: (0, 23), 1: (0, 48)},
  35: {11: (1, 54), 14: (1, 54), 1: (1, 54)},
  36: {11: (1, 55), 14: (1, 55), 1: (0, 49)},
  37: {11: (1, 22)},
  38: {17: (0, 41), 18: (0, 50)},
  39: {11: (1, 10), 1: (0, 51), 12: (0, 36), 14: (0, 23)},
  40: {11: (1, 9)},
  41: {1: (0, 52)},
  42: {11: (1, 29), 14: (1, 29), 1: (1, 29)},
  43: {11: (1, 8), 12: (0, 21), 13: (0, 53), 1: (0, 54), 14: (0, 23)},
  44: {11: (1, 13), 12: (0, 36), 1: (0, 55), 14: (0, 23)},
  45: {11: (1, 16), 12: (0, 21), 1: (0, 56), 13: (0, 57), 14: (0, 23)},
  46: {11: (1, 11)},
  47: {11: (1, 17), 1: (0, 58), 12: (0, 36), 14: (0, 23)},
  48: {11: (1, 21)},
  49: {11: (1, 52), 14: (1, 52), 1: (1, 52)},
  50: {11: (1, 30), 1: (1, 30), 14: (1, 30)},
  51: {11: (1, 18)},
  52: {19: (0, 59), 20: (0, 60), 21: (0, 61), 22: (0, 62), 23: (0, 63), 24: (0, 64), 25: (0, 65), 26: (0, 66), 27: (0, 67), 28: (0, 68), 29: (0, 69), 30: (0, 70)},
  53: {11: (1, 24), 12: (0, 36), 14: (0, 23), 1: (0, 71)},
  54: {11: (1, 3)},
  55: {11: (1, 7)},
  56: {11: (1, 4), 12: (0, 21), 13: (0, 72), 14: (0, 23), 1: (0, 73)},
  57: {11: (1, 25), 14: (0, 23), 12: (0, 36), 1: (0, 74)},
  58: {11: (1, 5)},
  59: {10: (0, 75)},
  60: {1: (1, 35)},
  61: {1: (1, 37)},
  62: {10: (0, 76)},
  63: {10: (0, 77)},
  64: {19: (0, 59), 31: (0, 78), 20: (0, 60), 21: (0, 61), 22: (0, 62), 23: (0, 63), 28: (0, 79), 25: (0, 65), 26: (0, 66), 27: (0, 67), 29: (0, 69), 30: (0, 70)},
  65: {1: (1, 36)},
  66: {10: (0, 80)},
  67: {1: (1, 38)},
  68: {1: (0, 81)},
  69: {1: (1, 34)},
  70: {10: (0, 82)},
  71: {11: (1, 27)},
  72: {11: (1, 0), 1: (0, 83), 14: (0, 23), 12: (0, 36)},
  73: {11: (1, 19)},
  74: {11: (1, 26)},
  75: {15: (0, 28), 16: (0, 84)},
  76: {17: (0, 41), 18: (0, 85)},
  77: {15: (0, 28), 16: (0, 86)},
  78: {11: (1, 33), 1: (1, 33), 14: (1, 33)},
  79: {1: (0, 87)},
  80: {15: (0, 28), 16: (0, 88)},
  81: {26: (1, 60), 30: (1, 60), 31: (1, 60), 23: (1, 60), 19: (1, 60), 22: (1, 60)},
  82: {15: (0, 28), 16: (0, 89)},
  83: {11: (1, 12)},
  84: {1: (1, 49)},
  85: {1: (1, 39)},
  86: {1: (1, 40)},
  87: {26: (1, 61), 30: (1, 61), 31: (1, 61), 23: (1, 61), 19: (1, 61), 22: (1, 61)},
  88: {1: (1, 50)},
  89: {17: (0, 90)},
  90: {1: (0, 91), 32: (0, 92)},
  91: {19: (0, 59), 22: (0, 62), 33: (0, 93), 27: (0, 67), 28: (0, 94), 34: (0, 95), 30: (0, 70), 35: (0, 96), 20: (0, 60), 21: (0, 61), 23: (0, 63), 31: (0, 97), 2: (0, 98), 25: (0, 65), 26: (0, 66), 29: (0, 69), 36: (0, 99)},
  92: {1: (0, 100)},
  93: {1: (1, 43)},
  94: {1: (1, 42)},
  95: {1: (1, 41)},
  96: {10: (0, 101)},
  97: {1: (1, 45)},
  98: {10: (0, 102)},
  99: {1: (1, 62)},
  100: {19: (0, 59), 22: (0, 62), 36: (0, 103), 33: (0, 93), 31: (0, 104), 27: (0, 67), 28: (0, 94), 34: (0, 95), 30: (0, 70), 35: (0, 96), 20: (0, 60), 21: (0, 61), 23: (0, 63), 2: (0, 98), 25: (0, 65), 26: (0, 66), 29: (0, 69)},
  101: {17: (0, 105)},
  102: {15: (0, 28), 16: (0, 106)},
  103: {1: (1, 63)},
  104: {1: (1, 44)},
  105: {37: (0, 107), 1: (0, 108)},
  106: {1: (1, 46)},
  107: {1: (0, 109)},
  108: {38: (0, 110), 39: (0, 111), 40: (0, 112)},
  109: {38: (0, 113), 39: (0, 111), 31: (0, 114), 40: (0, 112)},
  110: {1: (1, 65)},
  111: {40: (0, 115), 10: (0, 116)},
  112: {10: (1, 66), 40: (1, 66)},
  113: {1: (1, 64)},
  114: {1: (1, 47)},
  115: {10: (1, 67), 40: (1, 67)},
  116: {16: (0, 117), 15: (0, 28)},
  117: {1: (1, 48)},
}
TOKEN_TYPES = (
{0: 'preamble_statements',
 1: '_NEWLINE',
 2: 'SCOPE',
 3: '__anon_plus_1',
 4: 'language_syntax',
 5: 'NAME',
 6: 'master_scope_name_statement',
 7: 'target_language_name_statement',
 8: 'language_construct_rules',
 9: 'CONTEXTS',
 10: 'COLON',
 11: '$END',
 12: 'miscellaneous_language_rules',
 13: '__anon_star_0',
 14: '__ANON_0',
 15: '__ANON_2',
 16: 'free_input_string',
 17: 'LBRACE',
 18: 'indentation_block',
 19: 'POP',
 20: 'push_statement',
 21: 'pop_statement',
 22: 'PUSH',
 23: 'INCLUDE',
 24: '__anon_plus_2',
 25: 'match_statement',
 26: '__ANON_1',
 27: 'meta_scope_statement',
 28: 'statements_list',
 29: 'include_statement',
 30: 'MATCH',
 31: 'RBRACE',
 32: '__anon_star_3',
 33: 'capturing_block',
 34: 'scope_name',
 35: 'CAPTURES',
 36: 'match_statements',
 37: '__anon_plus_4',
 38: 'capturing_lines',
 39: '__anon_plus_5',
 40: 'INTEGER'}
)
parse_table.states = {s: {TOKEN_TYPES[t]: (a, RULES[x] if a is Reduce else x) for t, (a, x) in acts.items()}
                      for s, acts in STATES.items()}
parse_table.start_state = 0
parse_table.end_state = 16
class Lark_StandAlone:
  def __init__(self, transformer=None, postlex=None):
     callback = parse_tree_builder.create_callback(transformer=transformer)
     callbacks = {rule: getattr(callback, rule.alias or rule.origin, None) for rule in RULES.values()}
     self.parser = _Parser(parse_table, callbacks)
     self.postlex = postlex
  def parse(self, stream):
     tokens = lex(stream)
     sps = CON_LEXER.set_parser_state
     if self.postlex: tokens = self.postlex.process(tokens)
     return self.parser.parse(tokens, sps)
